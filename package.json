{
  "name": "hdr-glow",
  "version": "1.0.0",
  "description": "HDR Glow Avatar Generator - Convert images to glowing HDR PNGs for social media",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html hdr-converter.js 2020_profile.icc favicon.png demo.png demo-sdr.png dist/",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {}
}
